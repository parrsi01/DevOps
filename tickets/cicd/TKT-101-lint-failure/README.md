# TKT-101: CI Lint Failure

Reproduce by introducing a known lint error and pushing a branch.

Debug: open GitHub Actions `Lint` job logs; reproduce locally with `npm ci && npm run lint`.

Fix: correct the file/line or update lint config intentionally.
