# TKT-102: Test Fails on One Node Version

Reproduce by using a Node feature unsupported by one matrix version.

Debug: compare `Test (Node 20)` vs `Test (Node 22)` logs, reproduce with local `nvm` version.

Fix: make code compatible or update supported matrix versions.
